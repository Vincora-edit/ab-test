# 🚀 Быстрый деплой (как в вашем примере)

## Аналогично вашему Docker деплою

Вы показали пример где другой чат деплоит через SSH:
```bash
scp -P port file.tar.gz user@91.222.239.217:/path/
ssh user@host "docker load..."
```

## Точно так же для ab-test-generator.html:

### Вариант 1: Одна команда (PowerShell)

```powershell
# Замените данные на свои:
scp -P 22 "ab-test-generator.html" ваш-логин@ваш-домен.ru:/public_html/ab-test-generator.html
```

### Вариант 2: Автоматический скрипт

1. Откройте `deploy-timeweb-direct.ps1`
2. Измените первые строки:
```powershell
$SSH_USER = "ваш-логин"           # ← ваш логин
$SSH_HOST = "ваш-домен.ru"        # ← ваш домен или IP
$REMOTE_PATH = "/public_html"      # ← путь на сервере
```

3. Запустите:
```powershell
.\deploy-timeweb-direct.ps1
```

### Вариант 3: Bash (для Git Bash)

```bash
chmod +x deploy-simple.sh
./deploy-simple.sh
```

## Проверка SSH доступа

Сначала проверьте что SSH работает:
```powershell
ssh ваш-логин@ваш-домен.ru "pwd"
```

Если работает - деплой будет точно таким же как в вашем примере!

## Для автоматизации через ваш дашборд

Если ваш дашборд деплоит Docker образы, можно добавить туда команду:

```javascript
// В вашем дашборде добавьте:
const deployABTest = () => {
    exec(`scp -P 22 ab-test-generator.html user@host:/public_html/`)
}
```

---

**Итого:** Точно так же как в примере, только вместо `docker save` и `docker load` - просто копируем HTML файл через `scp`.

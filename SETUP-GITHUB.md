# 🎯 Настройка автодеплоя через GitHub

## Шаг 1: Создание SSH ключа (если нет)

### Windows (PowerShell):
```powershell
# Генерация SSH ключа
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"

# Просто нажимайте Enter на все вопросы (или укажите пароль)
```

### Просмотр публичного ключа:
```powershell
cat ~/.ssh/id_rsa.pub
```

### Просмотр приватного ключа:
```powershell
cat ~/.ssh/id_rsa
```

## Шаг 2: Добавление публичного ключа на сервер Timeweb

### Вариант A: Через SSH
```bash
# Подключитесь к серверу
ssh ваш-логин@ваш-домен.ru

# Создайте директорию для ключей (если нет)
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Добавьте публичный ключ
nano ~/.ssh/authorized_keys
# Вставьте содержимое id_rsa.pub, сохраните (Ctrl+O, Ctrl+X)

# Установите права
chmod 600 ~/.ssh/authorized_keys
```

### Вариант B: Через панель Timeweb
1. Зайдите в панель управления Timeweb
2. Найдите раздел "SSH ключи"
3. Добавьте публичный ключ (содержимое `id_rsa.pub`)

## Шаг 3: Проверка SSH подключения

```powershell
# Должно подключиться БЕЗ запроса пароля
ssh ваш-логин@ваш-домен.ru "pwd"
```

Если всё OK - увидите путь на сервере. Если просит пароль - повторите Шаг 2.

## Шаг 4: Создание репозитория на GitHub

### Через веб-интерфейс:
1. Откройте https://github.com/new
2. Название: `ab-test-generator` (или любое)
3. Публичный или приватный - на ваш выбор
4. НЕ создавайте README, .gitignore (у нас уже есть)
5. Нажмите "Create repository"

### Через командную строку:
```powershell
cd "d:\Мои документы\cursor\Scripts"

# Уже выполнено: git init

# Добавьте все файлы
git add .
git commit -m "Initial commit: AB test generator"

# Подключите GitHub репозиторий (замените USERNAME и REPO!)
git remote add origin https://github.com/USERNAME/REPO.git

# Отправьте код
git branch -M main
git push -u origin main
```

## Шаг 5: Добавление секретов в GitHub

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"**
4. Добавьте следующие секреты:

### SSH_PRIVATE_KEY
```
# Содержимое файла ~/.ssh/id_rsa
# В PowerShell выполните:
cat ~/.ssh/id_rsa

# Скопируйте ВСЁ от -----BEGIN до END----- включительно
```

### SSH_HOST
```
ваш-домен.ru
# или IP: 91.222.239.217
```

### SSH_USER
```
ваш-логин
```

### SSH_PORT
```
22
```

### REMOTE_PATH
```
/public_html
# или /www или /httpdocs - зависит от вашего хостинга
```

## Шаг 6: Проверка автодеплоя

### Тест 1: Ручной запуск
1. В GitHub откройте **Actions**
2. Выберите workflow "🚀 Deploy to Timeweb"
3. Нажмите **"Run workflow"** → **"Run workflow"**
4. Подождите ~30 секунд
5. Должны увидеть зелёную галочку ✅

### Тест 2: Автоматический деплой
```powershell
# Измените файл
echo "<!-- test -->" >> ab-test-generator.html

# Закоммитьте
git add ab-test-generator.html
git commit -m "test: auto deploy"
git push

# Откройте GitHub Actions - деплой запустится автоматически!
```

## ✅ Готово!

Теперь при каждом `git push` файл автоматически загрузится на ваш сервер!

## 🎉 Использование

### Вариант 1: Через GitHub интерфейс
1. Откройте файл на GitHub
2. Нажмите "Edit" (карандаш)
3. Внесите изменения
4. Нажмите "Commit changes"
5. Деплой запустится автоматически!

### Вариант 2: Локально
```powershell
# Внесите изменения в ab-test-generator.html
# Затем:
git add .
git commit -m "update generator"
git push
```

### Вариант 3: Кнопка деплоя
Просто откройте Actions → Run workflow!

## 🐛 Решение проблем

### "Permission denied (publickey)"
- Проверьте что публичный ключ добавлен на сервер
- `ssh ваш-логин@ваш-домен.ru` должен работать без пароля

### "Host key verification failed"
```powershell
ssh-keyscan -H ваш-домен.ru >> ~/.ssh/known_hosts
```

### Workflow падает с ошибкой
- Проверьте все секреты в Settings → Secrets
- Убедитесь что `REMOTE_PATH` существует на сервере
- Посмотрите логи в Actions для подробностей

---

**После настройки вы сможете просто говорить "деплой" и я буду пушить изменения в GitHub, а GitHub автоматически загрузит всё на сервер!**

# ⚡ Быстрая настройка за 5 минут

## Что вы получите:
Скажете "деплой" → я запушу в GitHub → GitHub автоматически загрузит на Timeweb

---

## 🚀 Шаг 1: Создайте репозиторий на GitHub (2 мин)

1. Откройте: https://github.com/new
2. Название: `ab-test-generator`
3. Приватный или публичный - на ваш выбор
4. НЕ добавляйте README
5. Нажмите **Create repository**

---

## 🔑 Шаг 2: Настройте SSH (только если нет ключа)

### Проверьте есть ли ключ:
```powershell
cat ~/.ssh/id_rsa.pub
```

Если выдало ошибку - создайте ключ:
```powershell
ssh-keygen -t rsa -b 4096 -C "your@email.com"
# Нажимайте Enter на все вопросы
```

### Добавьте публичный ключ на Timeweb:

**Способ A: Через панель Timeweb**
1. Зайдите в панель Timeweb
2. SSH ключи → Добавить ключ
3. Вставьте содержимое `~/.ssh/id_rsa.pub`

**Способ B: Через SSH**
```bash
ssh ваш-логин@ваш-домен.ru
mkdir -p ~/.ssh && chmod 700 ~/.ssh
nano ~/.ssh/authorized_keys
# Вставьте публичный ключ, Ctrl+O, Ctrl+X
chmod 600 ~/.ssh/authorized_keys
exit
```

### Проверка (должно подключиться БЕЗ пароля):
```powershell
ssh ваш-логин@ваш-домен.ru "pwd"
```

---

## 📦 Шаг 3: Залейте код на GitHub (1 мин)

```powershell
cd "d:\Мои документы\cursor\Scripts"

# Замените на ВАШИ данные:
git remote add origin https://github.com/ВАШ-USERNAME/ab-test-generator.git

# Отправьте код
git branch -M main
git push -u origin main
```

---

## 🔐 Шаг 4: Добавьте секреты в GitHub (2 мин)

1. На GitHub откройте ваш репозиторий
2. **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret** для каждого:

| Название | Значение | Как получить |
|----------|----------|--------------|
| `SSH_PRIVATE_KEY` | Приватный SSH ключ | `cat ~/.ssh/id_rsa` (скопируйте ВСЁ) |
| `SSH_HOST` | `ваш-домен.ru` | Ваш домен или IP |
| `SSH_USER` | `логин` | SSH логин от Timeweb |
| `SSH_PORT` | `22` | Обычно 22 |
| `REMOTE_PATH` | `/public_html` | Путь к корню сайта |

---

## ✅ Шаг 5: Тест деплоя (30 сек)

1. На GitHub откройте **Actions**
2. Выберите "🚀 Deploy to Timeweb"
3. Нажмите **Run workflow** → **Run workflow**
4. Подождите 30 секунд
5. Зелёная галочка ✅ = всё работает!

Откройте: `https://ваш-домен.ru/ab-test-generator.html`

---

## 🎉 ГОТОВО!

Теперь просто скажите мне **"деплой"** и я:
1. Запушу изменения в GitHub
2. GitHub автоматически загрузит файл на Timeweb
3. Через 30 секунд обновится live версия!

---

## 💡 Как использовать дальше:

### Вариант 1: Скажите "деплой"
```
Вы: деплой
Я: *git push* → GitHub Actions → Сервер ✅
```

### Вариант 2: Ручной пуш
```powershell
git add .
git commit -m "update"
git push
# Деплой запустится автоматически!
```

### Вариант 3: Через GitHub
1. Откройте файл на GitHub
2. Нажмите карандаш (Edit)
3. Внесите изменения
4. Commit changes
5. Деплой автоматически!

---

## 🐛 Если что-то не работает:

### SSH не подключается без пароля:
```powershell
ssh-copy-id ваш-логин@ваш-домен.ru
```

### GitHub Actions падает:
- Проверьте все 5 секретов в Settings → Secrets
- Убедитесь что `REMOTE_PATH` существует на сервере
- Посмотрите логи ошибки в Actions

### Нужна помощь:
Просто скажите "проблема с деплоем" и покажите ошибку!

---

**Всё! Настройка заняла 5 минут, теперь деплой занимает 10 секунд! 🚀**

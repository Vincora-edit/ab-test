# 🚀 AB Test Platform - Инструкция по использованию

Платформа для A/B тестирования - альтернатива Google Optimize с автоматическим деплоем.

## ⚡ Быстрый старт

### 1. Запустите сервер

```bash
python ab-test-api.py
```

Сервер запустится на `http://localhost:9999`

### 2. Откройте админ-панель

Перейдите на: `http://localhost:9999/ab-test-admin.html`

**Важно:** При первом открытии для вас автоматически создастся уникальный Client ID. Он сохранится в localStorage браузера.

### 3. Создайте тест

1. Нажмите **"➕ Создать новый тест"**
2. Заполните форму:
   - **Название теста**: например "Тест главной страницы"
   - **Исходная страница**: `/` или `/test-page`
   - **Тестовая страница**: `/test-variant` или `/test-page-1`
   - **Процент трафика**: 50 (половина пользователей)
   - **Режим URL**: `exact` (точное совпадение) или `contains` (содержит)
   - **UTM условия** (опционально): например, показывать тест только для utm_source=google

3. Нажмите **"💾 Сохранить"**

### 4. Скопируйте код установки

В верхней части админ-панели вы увидите код для установки:

```html
<script>window.AB_TEST_ID='client_xxxxxx';</script>
<script src="https://cdn.jsdelivr.net/gh/Vincora-edit/ab-test@main/ab-test-client.js"></script>
```

Нажмите **"📋 Копировать код"**

### 5. Установите на сайт

Вставьте скопированный код в `<head>` вашего сайта **ПЕРЕД** кодом Яндекс.Метрики.

**Пример:**

```html
<head>
    <meta charset="UTF-8">
    <title>Мой сайт</title>

    <!-- AB Test код - ПЕРЕД Метрикой! -->
    <script>window.AB_TEST_ID='client_1234567890_abc123';</script>
    <script src="https://cdn.jsdelivr.net/gh/Vincora-edit/ab-test@main/ab-test-client.js"></script>

    <!-- Яндекс.Метрика -->
    <script>/* код метрики */</script>
</head>
```

### 6. Готово! 🎉

Тест начнёт работать автоматически:
- 50% пользователей увидят исходную страницу
- 50% будут перенаправлены на тестовую страницу
- Данные будут отправляться в Яндекс.Метрику и Roistat

## 🔄 Как работает автодеплой

1. Вы создаёте/редактируете тест в админ-панели → нажимаете "Сохранить"
2. Тест сохраняется в `localStorage` вашего браузера
3. **Автоматически** отправляется POST запрос на сервер `http://91.222.239.217:9999/save-tests`
4. Сервер сохраняет файл `tests/client_XXXXXX.json`
5. **Автоматически** выполняется:
   ```bash
   git add tests/
   git commit -m "Update tests for client_XXX (1 tests)"
   git push
   ```
6. Через 1-2 минуты файл становится доступен через CDN:
   `https://cdn.jsdelivr.net/gh/Vincora-edit/ab-test@main/tests/client_XXXXXX.json`
7. Скрипт на вашем сайте загружает конфигурацию и применяет тест

## 📊 Как смотреть статистику

### В Яндекс.Метрике

1. Зайдите в свой счётчик Яндекс.Метрики
2. **Отчёты** → **Стандартные отчёты** → **Параметры визитов**
3. Добавьте фильтр по параметру `ab_test_id`
4. Вы увидите:
   - `ab_test_id` - ID теста (например, `demo_test_1`)
   - `ab_variant` - вариант (`original` или `test`)

### В Roistat

События автоматически отправляются с параметрами:
- `test_id` - ID теста
- `variant` - вариант теста

## 🎯 Примеры использования

### Пример 1: Тест главной страницы

- **Исходная**: `/`
- **Тестовая**: `/main-v2`
- **Трафик**: 50%
- **URL Match**: exact

Результат: половина посетителей главной страницы будет перенаправлена на `/main-v2`

### Пример 2: Тест только с Google Ads

- **Исходная**: `/landing`
- **Тестовая**: `/landing-v2`
- **Трафик**: 50%
- **UTM условие**: `utm_source` contains `google`

Результат: тест сработает только для посетителей с `?utm_source=google` в URL

### Пример 3: Тест раздела каталога

- **Исходная**: `/catalog/products`
- **Тестовая**: `/catalog/products-new`
- **Трафик**: 30%
- **URL Match**: contains

Результат: тест сработает для всех URL, содержащих `/catalog/products`

## 🔧 Настройка UTM условий

Вы можете запускать тесты только для определённого трафика:

1. **exists** - параметр должен существовать
2. **equals** - точное совпадение (utm_source = google)
3. **contains** - содержит (utm_source содержит "google")

**Пример:**
```
utm_source contains "yandex"
```

Тест сработает для:
- `?utm_source=yandex`
- `?utm_source=yandex_direct`
- `?utm_source=my_yandex_campaign`

## ⚙️ Технические детали

### Как распределяется трафик

Используется hash-функция от `user_id + test_id`:
```javascript
var hash = hashCode(userId + test.id);
var percentage = (hash % 100) + 1;
var variant = percentage <= traffic_percent ? 'test' : 'original';
```

Это гарантирует:
- ✅ Стабильное распределение (один пользователь всегда видит один вариант)
- ✅ Равномерное распределение (близко к 50/50)
- ✅ Детерминированность (можно воспроизвести результат)

### Как работают cookie

- Cookie сохраняется на N дней (по умолчанию 30)
- Имя: `ab_test_{test_id}`
- Значение: `original` или `test`

### Нормализация URL

Все URL нормализуются для корректного сравнения:
- `""` → `/`
- `/page/` → `/page`
- `/` остаётся `/`

## 🐛 Отладка

Включите консоль браузера (F12) и смотрите логи:

```
[AB Test] Loading tests from: https://cdn.jsdelivr.net/gh/...
[AB Test] Loaded tests: [...]
[AB Test] Current page: /test-page
[AB Test] User ID: user_1234567890_abc123
[AB Test] Found matching test: Demo Test
[AB Test] New variant assigned: test percentage: 42
[AB Test] Redirecting to: /test-page-1
```

## 📝 Бэкап

Кнопка **"📥 Скачать JSON"** создаст локальную копию всех ваших тестов в формате JSON.

Используйте это для:
- Бэкапа конфигурации
- Переноса тестов на другой сервер
- Отладки

## 🔒 Безопасность

- HTTPS через jsDelivr CDN
- CORS настроен для работы с любых доменов
- Client ID хранится только в localStorage
- Каждый клиент видит только свои тесты

## 💡 Советы

1. **Запускайте по одному тесту** - не создавайте несколько активных тестов для одной страницы
2. **Ждите достаточно данных** - минимум 100-200 конверсий на вариант
3. **Тестируйте значимые изменения** - мелкие правки (цвет кнопки) не дадут статистически значимых результатов
4. **Используйте UTM** - тестируйте гипотезы на конкретном источнике трафика

## 🆘 Помощь

При проблемах проверьте:

1. ✅ Сервер запущен: `python ab-test-api.py`
2. ✅ Код установлен ПЕРЕД Метрикой
3. ✅ Client ID правильный
4. ✅ Тест активен (зелёный переключатель)
5. ✅ URL совпадает (проверьте режим exact/contains)
6. ✅ В консоли нет ошибок

---

**Готово!** Теперь у вас есть полноценная платформа для A/B тестирования! 🎉

# 🚀 Генератор A/B Тест Скрипта

Автоматический генератор скриптов для A/B тестирования на Tilda и других платформах.

## ✨ Возможности

- 🎨 Красивый веб-интерфейс для генерации скриптов
- ⚡ Мгновенные редиректы без двойного счёта просмотров
- 📊 Интеграция с Яндекс.Метрикой и Roistat
- 🍪 Cookie-based персистентность пользователей
- 🎯 Точное распределение трафика через хеширование
- 🔧 Гибкая настройка UTM условий

## 🌐 Live Demo

**Генератор:** https://ваш-домен.ru/ab-test-generator.html

## 🚀 Быстрый деплой

### Метод 1: Автоматический (GitHub Actions)

1. **Первая настройка:**
   ```bash
   # Добавьте SSH ключ в GitHub Secrets
   cat ~/.ssh/id_rsa  # скопируйте содержимое
   ```

2. **В GitHub:**
   - Откройте Settings → Secrets and variables → Actions
   - Добавьте секреты:
     - `SSH_PRIVATE_KEY` - ваш приватный SSH ключ
     - `SSH_HOST` - домен или IP сервера (например: `example.com`)
     - `SSH_USER` - SSH логин
     - `SSH_PORT` - порт SSH (обычно `22`)
     - `REMOTE_PATH` - путь на сервере (например: `/public_html`)

3. **Деплой:**
   ```bash
   git add .
   git commit -m "update generator"
   git push
   ```

   Или просто нажмите кнопку **"Run workflow"** в GitHub Actions!

### Метод 2: Ручной деплой (PowerShell)

```powershell
.\deploy-timeweb-direct.ps1
```

### Метод 3: Одна команда

```powershell
scp -P 22 ab-test-generator.html user@host:/public_html/
```

## 📁 Структура проекта

```
Scripts/
├── ab-test-generator.html      # Основной генератор (деплоится)
├── vincora-ab-test-v4-fixed.html  # Финальная версия скрипта
├── deploy-timeweb-direct.ps1   # Ручной деплой (Windows)
├── deploy-simple.sh            # Ручной деплой (Linux/Mac)
├── .github/workflows/deploy.yml   # GitHub Actions
└── README.md                   # Документация
```

## 🔧 Использование генератора

1. Откройте `ab-test-generator.html` в браузере
2. Заполните форму:
   - URL оригинальной страницы
   - URL тестовой страницы
   - Процент трафика на тест
   - UTM условия
3. Нажмите "Сгенерировать код"
4. Скопируйте готовый скрипт
5. Вставьте в `<head>` вашего сайта (ПЕРЕД Метрикой!)

## 📊 Проверка в Яндекс.Метрике

1. Отчёты → Источники → Страницы входа
2. Должны видеть ТОЛЬКО финальные страницы (без цепочек редиректов)
3. Параметры визитов → найдите `ab_variant`
4. Создайте сегменты для сравнения конверсий

## 🛠️ Разработка

```bash
# Установка зависимостей для деплоя
npm install

# Деплой через Node.js
npm run deploy

# Запуск HTTP API для деплоя
npm run deploy:server
```

## 📝 Версии скрипта

- **v1** - Базовая версия с редиректами
- **v2** - Добавлена отправка в аналитику
- **v3** - Polling для ожидания загрузки счётчиков
- **v4** - **ТЕКУЩАЯ** - Исправлено точное сопоставление URL

## 🤝 Контрибьютинг

Pull requests приветствуются!

## 📄 Лицензия

MIT

---

**Создано с ❤️ для маркетологов и веб-мастеров**
